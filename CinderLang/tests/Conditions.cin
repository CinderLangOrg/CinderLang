namespace Test.Conditions
{
	variadic extern def printf(byte* data) : int;

	def Test() 
	{
		bool a = true;

		if (a == a) printf("True");
		if (a) printf("Test");

		else printf("False");
	}
}