namespace Test.EntryPoint
{
	variadic extern def printf(byte* data) : int;

	def main() : int
	{
		int a = 10;

		while(a > 0)
		{
			a = a - 1;

			printf("loop! ");
			
			return 0;
		}

		return 0;
	}
}